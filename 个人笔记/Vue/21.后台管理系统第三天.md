# 21 .后台管理系统第三天

## 三级分类信息组件的搭建

### 1 搭建对应的html结构和样式

### 2 下拉列表框的注意点

- option的value值就是最终select的value值

### 3 调用对应的一级分类信息的api接口函数,获取到一级分类列表的信息数据数组,然后在对应的结构再进行遍历展示

### 4 当一级分类信息选中时,在该选中列表中绑定一个change的自定义事件会被触发,该自定义事件中调用对应的请求二级分类列表的api接口函数,获取到对应的二级分类信息的数组数据,然后在页面中进行展示

### 5 当二级分类信息选中时,在该选中列表中绑定一个change的自定义事件会被触发,该自定义事件中调用对应的请求三级分类列表的api接口函数,获取到对应的三级分类信息的数组数据,然后在页面中进行展示

### 6 当三级分类信息选中时,分发属性详情组件的自定义事件,该自定义事件根据一.二.三级分类的id来获取到属性详情的信息数据数组

### 注意点

- 1 在改变二级或者一级分类信息选中数据时,需要对三级或者二级和三级的信息进行清空

## 属性详情页组件

### 1 搭建对应的html结构和样式

### 2 定义对应的根据一级二级三级id获取的属性详情的自定义事件,该自定义事件在分发后,会请求对应的接口,获取到数据,然后在页面中进行展示出来

### 3 在对应的结构中使用作用域插槽然后再对二级数据进行展示,作用域插槽是需要父级组件有传递动态数据,使用作用域插槽后得到的数据,就是当前行的数据(row).可以使用{row,$index}更加方便

### 4 给删除按钮绑定事件,该事件用来显示修改属性的页面,隐藏当前属性详情的展示页,并且把当前行的数据作为参数传递过去,让修改属性的页面能够使用该组件,在复制数据时,使用深拷贝

## 属性值修改页组件

### 1 搭建对应的结构和样式

### 2 根据属性详情页获取到的数据,将数据在页面中进行展示,使用作用域插槽,将数据进行展示

### 3 给对应的删除按钮,绑定事件.用来删除一个属性值,在删除时,传递下标,使用数组的splice方法,进行删除

### 子主题 4

## 拷贝

### 

### 子主题 2

